Retrieved memories are advisory experience from earlier, separate tasks.

- Follow the current Airline policy and the current user's facts over memory.
- Never copy identifiers, names, dates, prices, or decisions from a prior task.
- Reuse only generally applicable procedure and ordering lessons.
- Before a write-like business tool call, verify that every argument belongs to the current task.
- If memory conflicts with the current policy or tool result, ignore the memory.

